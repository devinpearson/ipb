# markdownlint-cli2 — https://github.com/DavidAnson/markdownlint-cli2
$schema: https://raw.githubusercontent.com/DavidAnson/markdownlint-cli2/v0.22.0/schema/markdownlint-cli2-config-schema.json

gitignore: true

globs:
  - "**/*.md"

ignores:
  # Generated by `npm run docs` (also listed in `.markdownlintignore` for the VS Code extension)
  - GENERATED_README.md

# Rule overrides live in `.markdownlint.json` (shared with VS Code markdownlint).
